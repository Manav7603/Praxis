# Core dependencies — pin versions in production; use pyproject.toml for dev
numpy>=1.24
torch>=2.0
tqdm>=4.65
pyyaml>=6.0
einops>=0.7
safetensors>=0.4
tokenizers>=0.15
datasets>=2.14
tensorboard>=2.14
